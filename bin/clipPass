#!/bin/bash

# get password from bitwarden CLI, pipe it straight to clipboard

PASSWD=$(bw get password "$1")

if [ 0 = "$?" ] ; then
	echo -n "$PASSWD" | xclip -sel clip 
else 
	echo >&2 "FAILED"
	exit 1
fi
